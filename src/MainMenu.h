#pragma once

#include <vector>
#include <string>
#include "Renderer.h"

/// @brief Class that manages main menu and can download levels.
class MainMenu
{
private:
    std::vector<std::string> menu_items;
public:
    MainMenu();


};

